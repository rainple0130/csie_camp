/**
 * BasePath 配置
 * 在生產環境（GitHub Pages）使用 /csie_camp
 * 在開發環境使用空字串
 */
export const BASE_PATH = "/csie_camp";
//  typeof process !== "undefined" && process.env.NODE_ENV === "production"
    // ? "/csie_camp"
    // : "";
